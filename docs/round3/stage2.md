# 2단계: Memory 크기 실험 + 실패 관찰

**목표**: `MAX_MESSAGES`를 바꿔 가며 같은 질문 시퀀스를 돌리고, 입력 토큰 / 정확도를 정량 비교한다.

## 실험 설계

| 실험 | MAX_MESSAGES | 목적 |
|---|---|---|
| A | `20` (기준) | 정상 케이스 |
| B | `2` (극단적으로 작게) | 지시 대명사 해결 실패 관찰 |
| C | `Integer.MAX_VALUE` (사실상 무제한) | 입력 토큰의 선형 증가 관찰 |

### 10턴 시나리오 (각 실험 공통)

```bash
SESSION="exp-$(date +%s)"

# 1) 배달 상황 조회
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"2024-1234 배달 상황 알려주세요"}'

# 2) 지시 대명사
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"그거 몇 분 남았어요?"}'

# 3) 다른 주문
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"2024-1235 주문도 있는데 메뉴 뭐였죠?"}'

# 4) 지시 대명사
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"아 그 버거 세트"}'

# 5) 취소 가능 질문
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"2024-1234 취소 가능해요?"}'

# 6) 다른 주문 취소 가능?
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"그럼 1235는 취소되죠?"}'

# 7) 취소 요청 ("그거" = 1235)
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"그거 취소해주세요"}'

# 8) 다시 1234 조회
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"아까 1234는 언제 도착해요?"}'

# 9) 라이더 위치
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"그 주문 라이더 위치 다시 확인"}'

# 10) 요약
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: $SESSION" \
  -d '{"message":"요약해 주세요 지금까지 제가 뭘 물어봤는지"}'
```

---

## 정량 비교 표

> 입력 토큰: `PerformanceLoggingAdvisor`의 `입력 토큰: XX` 로그 평균
> 지시 대명사 성공: 2/6/7/8/9번 턴에서 올바른 orderId가 Tool 파라미터로 전달된 횟수

| 실험 | MAX_MESSAGES | 10턴 평균 입력 토큰 | 10턴 평균 출력 토큰 | 10턴 평균 응답 시간 | 지시 대명사 해결 성공 / 5 |
|---|---|---|---|---|---|
| A | 20 | 1523 | 97 | 5017ms | 5/5 |
| B | 2 | 1158 | 89 | 4865ms | 4/5 |
| C | MAX_VALUE | 1157 | 82 | 4015ms | 5/5 |

> 지시 대명사 성공 기준: 2/6/7/8/9번 턴에서 orderId가 올바르게 해석됐는지 (Tool 호출 여부와 무관). Tool 미호출은 재사용 지시의 영향이므로 별도 집계.

### 실험 A (MAX_MESSAGES = 20) 턴별 토큰 상세

<details>
<summary>금지 조항 포함 ASSISTANT_PROMPT 버전 (참고용)</summary>

금지 조항(타 플랫폼 비교 금지, 개인정보 노출 금지, 쿠폰 약속 금지 등)을 추가했더니 프롬프트가 1273 → 2728 토큰으로 늘었고 qwen2.5의 Tool calling 안정성이 깨졌다. Turn 1 중국어 응답, Turn 5~9 portun 재발. 실험 데이터로 쓸 수 없어 금지 조항을 제거하고 재실행했다.

| 턴 | 질문 요약 | 입력 토큰 | 출력 토큰 | 응답 시간(ms) | 지시 대명사 성공 |
|---|---|---|---|---|---|
| 1 | 1234 배달 상황 | 1530 | 98 | 10053 | - |
| 2 | 그거 몇 분 | 806 | 26 | 5003 | ❌ (되묻기) |
| 3 | 1235 메뉴 | 1889 | 189 | 7903 | - |
| 4 | 그 버거 세트 | 2132 | 135 | 6369 | ✓ (1235) |
| 5 | 1234 취소 가능? | 1115 | 54 | 2508 | - |
| 6 | 1235 취소되죠? | 1191 | 35 | 1481 | ❌ (portun) |
| 7 | 그거 취소 (1235) | 1241 | 36 | 1533 | ❌ (portun) |
| 8 | 아까 1234 도착 | 1300 | 29 | 1271 | ❌ (portun) |
| 9 | 그 주문 라이더 위치 | 1348 | 29 | 1270 | ❌ (portun) |
| 10 | 요약 | 1402 | 268 | 10177 | - |

</details>

| 턴 | 질문 요약 | 입력 토큰 | 출력 토큰 | 응답 시간(ms) | 지시 대명사 성공 |
|---|---|---|---|---|---|
| 1 | 1234 배달 상황 | 1290 | 91 | 8746 | - |
| 2 | 그거 몇 분 | 679 | 70 | 5972 | ✓ (1234, Memory) |
| 3 | 1235 메뉴 | 778 | 50 | 1998 | - |
| 4 | 그 버거 세트 | 1854 | 147 | 6387 | - |
| 5 | 1234 취소 가능? | 2094 | 108 | 5630 | - |
| 6 | 1235 취소되죠? | 2193 | 104 | 4850 | ✓ (1235, Tool) |
| 7 | 그거 취소 (1235) | 2378 | 128 | 5618 | ✓ (1235, Tool) |
| 8 | 아까 1234 도착 | 1211 | 104 | 4224 | ✓ (1234, Memory) |
| 9 | 그 주문 라이더 위치 | 1334 | 57 | 2327 | ✓ (1234, Memory) |
| 10 | 요약 | 1416 | 113 | 4420 | - |

> Turn 3: "2024-1235 주문도 있는데"처럼 명시적 주문번호가 있어도 되물었다. Turn 4 "아 그 버거 세트"에서야 getOrderDetail(1235) 호출.
> Turn 10: 요약 마지막에 중국어 한 줄 누출 ("还有什么其他问题需要帮助吗？"). 모델 fluke.

### 프롬프트 변경 경위와 트레이드오프

1단계에서 세션 하이재킹 시나리오를 테스트하다가, Memory에 위치 정보가 없는 상태에서 "그거 지금 어디쯤이야?"를 물었을 때 LLM이 존재하지 않는 주소("서울특별시 영등포구 선유동 157-6, 2층")를 만들어냈다. 이를 막기 위해 ASSISTANT_PROMPT에 금지 조항을 추가했다.

```
도구 응답에 없는 정보는 절대 추측하거나 만들어내지 마세요.
타 배달 플랫폼을 추천하거나 비교하지 마세요.
사장님·라이더의 개인정보를 노출하지 마세요.
쿠폰·할인·보상 지급을 약속하지 마세요.
```

결과: 할루시네이션은 막혔지만 프롬프트가 1273 → 2728 토큰으로 늘면서 qwen2.5의 Tool calling 안정성이 깨졌다(portun 재발, Turn 1 중국어 응답). 안전 규칙을 프롬프트에 넣는 것과 모델 안정성 사이의 트레이드오프다.

2단계 실험은 MAX_MESSAGES 효과를 측정하는 것이 목적이므로 금지 조항을 제거하고 진행했다. 현재 ASSISTANT_PROMPT(금지 조항 없음)에서 할루시네이션이 다시 발생하는지 아래에서 확인한다.

**할루시네이션 유도 테스트 (금지 조항 없는 현재 버전)**

Memory에 위치 정보가 없는 상태(getOrderDetail만 호출된 세션)에서 위치를 물으면 어떻게 되는지:

```bash
# 세션 재건 (order detail만, 위치 정보 없음)
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: hallucination-test" \
  -d '{"message":"2024-1234 주문 상태 확인해줘"}'

# 위치 질문 — 할루시네이션 발생 여부
curl -s -X POST http://localhost:8080/api/v1/assistant \
  -H "Content-Type: application/json" \
  -H "X-Session-Id: hallucination-test" \
  -d '{"message":"그거 지금 어디쯤이야?"}'
```

**세션 재건 응답**

```
주문번호 2024-1234의 상태는 배달 중입니다. 현재 주문은 교촌치킨 강남점에서 발송되어 배달이
진행 중이며 예상 도착 시간은 2026년 5월 30일 오후 9시 5분입니다.
주문한 메뉴와 금액은 다음과 같습니다:
- 허니콤보: 1개, 가격 23,000원 / 콜라 1.25L: 1개, 가격 3,000원
전체 주문 금액은 26,000원입니다.
```

**위치 질문 응답**

```
현재 배달원은 주문 번호 2024-1234의 배달을 진행 중이며, 위치는 강남역 인근입니다.
정확한 도착 시간은 아직 결정되지 않았지만, 최대한 빠른 시간 내에 고객님의 장소로 배달할 예정입니다.
만약 배송이 지연될 경우, 배달원과 연락하시면 더 자세한 정보를 얻으실 수 있습니다.
```

> 할루시네이션 확인됨. mock 데이터의 실제 riderLocation은 "역삼역 사거리 부근"인데 "강남역 인근"을
> 만들어냈다. "배달원과 연락하시면"이라는 부적절한 안내도 포함됐다.
> 금지 조항이 있을 때("정확한 위치 정보는 제공하지 않지만")와 없을 때("강남역 인근")의 차이다.
> qwen2.5에서 안전 규칙과 Tool calling 안정성은 트레이드오프다. 프롬프트가 길어질수록 안정성이 흔들린다.

---

## 실패 관찰: MAX_MESSAGES = 2의 파괴적 결과

> MAX_MESSAGES = 2 는 직전 USER 메시지 + 직전 ASSISTANT 메시지 **한 쌍**밖에 기억하지 못한다.
> 3턴 이상 넘어가면 처음 언급된 orderId는 이미 윈도우 밖으로 밀려난 상태다.

### 실패 캡처 — 지시 대명사가 엉뚱하게 해석된 턴

**턴 6 — "그럼 1235는 취소되죠?"**

요청:
```bash
curl -s -X POST http://localhost:8080/api/v1/assistant -H "Content-Type: application/json" -H "X-Session-Id: exp-b" -d '{"message":"그럼 1235는 취소되죠?"}'
```

응답:
```
어떤 주문을 말씀하시는 건가요? 최근에 언급된 주문번호인 "1235"는 직접 입력 형식(예: 2024-1235)으로 입력해주시면 확인 가능합니다.
```

Memory 상태 (턴 6 입력 시점, 윈도우에 남은 2개):
```json
[
  {
    "type": "USER",
    "content": "2024-1234 취소 가능해요?"
  },
  {
    "type": "ASSISTANT",
    "content": "2024-1234번 주문은 현재 배달 중이므로 취소가 불가능합니다. 이미 라이더가 출발하여 배달을 진행하고 있습니다. 다른 도움이 필요하시면 알려주세요!"
  }
]
```

콘솔 로그:
```
[Assistant] sessionId=exp-b, message=그럼 1235는 취소되죠?
LLM 호출 완료 — 2704ms | 입력 토큰: 676 | 출력 토큰: 55 | 총 토큰: 731
```

> T1~T4에서 1235가 여러 번 언급됐지만 MAX_MESSAGES=2 윈도우에는 T5(1234 취소 불가) 한 쌍만 남아 있었다. 1235의 맥락은 완전히 밀려난 상태. 모델이 "1235"라는 숫자 자체는 인식했지만 그게 어떤 주문인지 알 수 없어 형식이 틀렸다며 되물었다. Tool도 호출되지 않았다.

**턴 10 — "요약해 주세요 지금까지 제가 뭘 물어봤는지"**

응답:
```
최근에 다음과 같은 내용에 대해 질문하셨습니다:
1. 그 주문의 라이더 위치를 다시 확인했습니다.

라이더는 역삼역 사거리 부근에서 배달 중입니다.
```

Memory 상태 (턴 10 입력 시점):
```json
[
  {
    "type": "USER",
    "content": "그 주문 라이더 위치 다시 확인"
  },
  {
    "type": "ASSISTANT",
    "content": "라이더의 현재 위치를 재조회했습니다. 라이더는 여전히 역삼역 사거리 부근에서 배달 중입니다. 정확한 위치 정보는 앱을 통해 확인하실 수 있습니다."
  }
]
```

> 10턴에 걸친 대화 요약을 요청했는데 T9 한 쌍밖에 없으니 T9 내용만 돌려줬다. 1234 배달 조회, 1235 메뉴 확인, 취소 요청 같은 9턴이 통째로 소실됐다.

### 왜 2는 부족한가

```
메시지 구조 예시 (7번 턴 직전 상태):

  [윈도우에 있는 것]
  USER: "그럼 1235는 취소되죠?"        ← 6번 턴
  ASSISTANT: "네, 1235는 취소 가능합니다." ← 6번 턴 응답

  [윈도우 밖으로 밀려난 것]
  USER: "2024-1234 배달 상황..."        ← 1번 턴 (소실)
  ...
  USER: "2024-1235 주문도 있는데..."    ← 3번 턴 (소실)
  ...
```

MAX_MESSAGES = 2이면 USER/ASSISTANT 한 쌍(2개)만 남는다.
7번 턴에서 "그거"가 1235를 가리키는 맥락(3번 턴에서 1235 첫 언급)이
윈도우 밖으로 밀려나면, LLM은 1235가 어떤 주문인지 알 수 없다.

---

### 실험 C (MAX_VALUE) 응답 시간 선형 증가 관찰

| 턴 | 입력 토큰 | A 입력 토큰 | C/A 비율 | 응답 시간(ms) | A 응답 시간(ms) | C/A 비율 |
|---|---|---|---|---|---|---|
| 1 | 1291 | 1290 | 1.00x | 7518 | 8746 | 0.86x |
| 3 | 806 | 778 | 1.04x | 1941 | 1998 | 0.97x |
| 5 | 936 | 2094 | 0.45x | 2090 | 5630 | 0.37x |
| 7 | 2240 | 2378 | 0.94x | 4666 | 5618 | 0.83x |
| 10 | 1358 | 1416 | 0.96x | 5831 | 4420 | 1.32x |

C (Tool 미호출 턴) 입력 토큰 추이: T2 689 → T3 806 → T4 868 → T5 936 → T6 1009 → T8 1131 → T9 1238 → T10 1358

> 10턴 안에서는 C의 평균 입력 토큰(1157)이 A(1523)보다 낮게 나왔다. Tool 미호출 턴이 많아서다. T5(936)·T6(1009)는 A에서 Tool 호출로 2094·2193이 찍혔던 자리인데 C는 Memory에서 꺼내 처리해 오히려 적다.
>
> 하지만 Tool 미호출 턴만 추적하면 선형 증가가 보인다. T2→T10 구간에서 턴당 약 90~120 토큰씩 꾸준히 늘었다. MAX=20 제한이 없으니 윈도우가 계속 커지는 것이다. 이 속도라면 21턴부터는 A가 약 1400토큰대에 수렴하는 반면 C는 계속 올라간다.
>
> A 대비 응답 시간이 눈에 띄게 느려진 턴은 10턴 범위에서 없었다. C의 T10이 A 대비 1.32x로 소폭 느린 것이 처음이다. 명확한 역전은 20턴 이후 예상한다.

---

## 회고

### 회고 1 — T3은 명시적 번호가 있어도 실패했고, T4는 맥락만으로 맞혔다

A 실험 T3 "2024-1235 주문도 있는데 메뉴 뭐였죠?"에서 모델이 "어떤 주문인가요?"로 되물었다. 메시지에 "2024-1235"가 그대로 들어있는데도. T4 "아 그 버거 세트"에서야 `getOrderDetail(1235)`가 호출됐다.

이상한 점이 두 가지다. 먼저 명시적 번호가 있는데 왜 실패했냐. Memory에 1234 관련 대화가 쌓여 있으니 모델이 "이게 1234 메뉴를 묻는 건지, 1235 신규 조회인지"를 혼동한 것으로 보인다. 다음으로 "아 그 버거 세트"는 번호가 없는데 왜 맞혔냐. T3 실패 응답("어떤 주문인가요?")이 Memory에 들어가고, T4는 그 앞뒤 흐름에서 1235를 추론했다. 숫자를 명시하는 것보다 대화 흐름이 더 강한 신호로 작동한 셈이다.

C 실험(MAX_VALUE)에서도 T3이 실패했다. B 실험(MAX=2)에서는 T3이 맞혔다. Memory가 작아서 1234 맥락이 없으니 "2024-1235"만 보고 바로 호출한 것이다. 맥락이 많을수록 오히려 판단을 흐리게 할 수 있다.

---

### 회고 2 — B 실험에서 실패한 응답이 다음 턴의 단서가 됐다

T6 "그럼 1235는 취소되죠?"에서 "어떤 주문이요?"로 실패했다. 그런데 그 실패 응답 자체에 "1235"라는 숫자가 들어있었다. 그 T6 쌍이 Memory 윈도우에 남은 채로 T7 "그거 취소해주세요"를 받았더니 `cancelOrder(1235)`가 성공했다.

실패한 응답이 다음 턴의 맥락 단서가 된 것이다. T6이 조용히 실패해서 Memory에 1235 언급이 없었다면 T7도 실패했을 것이다. MAX=2 조건에서 벌어진 우연한 연쇄다.

---

### 회고 3 — Memory가 많을수록 입력 토큰이 줄어드는 역설

실험에서 가장 예상 밖이었던 건 C(MAX_VALUE)의 평균 입력 토큰(1157)이 A(1523)보다 낮다는 점이다. Memory가 무제한인데 왜 토큰이 더 적냐.

Memory가 많을수록 Tool 재호출이 줄기 때문이다. Memory에서 답을 꺼낼 수 있으면 Tool을 부르지 않는다. Tool 결과 JSON이 프롬프트에서 빠진다. 그 JSON이 텍스트 메시지 수십 개보다 토큰을 더 많이 잡아먹는다.

C에서 T5와 T6의 입력 토큰이 936, 1009인데 A에서는 같은 자리가 2094, 2193이다. A에서는 Tool을 불러서 높고, C에서는 Memory에서 꺼내서 낮다. Memory 크기와 입력 토큰의 관계는 단순하지 않다. "Memory 크기가 크면 토큰이 더 많다"는 건 Tool 미호출 턴에서만 성립하는 말이다.

---

### 회고 4 — T10 요약 실패: 모델이 모르는 걸 만들어내지 않았다

MAX=2에서 T10 "지금까지 뭘 물어봤는지 요약해줘"에 T9 내용 1개만 돌아왔다. 1234 배달 조회, 1235 메뉴 확인, 취소 요청 같은 9턴이 없는 것처럼 대답했다.

처음엔 모델이 없는 정보를 채워 넣을 거라고 예상했는데 그러지 않았다. Memory에 T9 한 쌍밖에 없으니 그게 전부라고 답했다. 환각이 아니라 알고 있는 것만 말한 것이다.

같은 실험에서 할루시네이션 유도 테스트(Memory에 위치 정보 없이 "지금 어디쯤이야?"를 물었을 때)에서는 "강남역 인근"을 만들어냈다. 요약 질문은 "모르면 모른다"고 할 수 있는 상황이었고, 위치 질문은 라이더 위치를 알아야 한다는 역할 기대가 있었다. 같은 모델인데 다른 반응이 나온 이유가 질문의 성격 차이였던 것 같다.

---

### 회고 5 — 금지 조항이 실험 자체를 망가뜨렸다

금지 조항을 추가했더니 Turn 1부터 중국어로 응답하거나 portun 오류가 재발했다. 그 데이터로는 MAX_MESSAGES 효과를 측정할 수 없어서 금지 조항을 빼고 다시 실행해야 했다.

안전을 강화하려다 기능이 망가진 것이다. 프롬프트 길이가 늘면서 qwen2.5의 Tool calling 안정성이 깨졌다. 규칙이 많을수록 모델이 더 잘 따를 거라는 기대가 이 모델에서는 통하지 않았다.

프로덕션이었다면 더 작은 모델을 별도로 써서 입력을 필터링하거나, 안전 규칙만 처리하는 레이어를 분리하는 방식을 검토했을 것이다. 하나의 모델에 역할을 많이 쌓는 것에는 한계가 있다.

---

## 설계 결정 문서

### MessageWindowChatMemory(슬라이딩 윈도우) vs 대화 요약 전략 비교

| 항목 | 슬라이딩 윈도우 | 대화 요약 |
|---|---|---|
| 동작 방식 | 최근 N개 메시지만 유지, 오래된 것은 버림 | 오래된 대화를 LLM이 요약해 압축 저장 |
| 구현 복잡도 | 낮음 (Spring AI 기본 제공) | 높음 (요약 시점 판단, 추가 LLM 호출 필요) |
| 토큰 비용 | 고정 (maxMessages 설정값에 비례) | 요약 시 추가 비용 발생, 이후엔 절약 |
| 정확도 | 최근 발화만 정확, 오래된 맥락 소실 | 압축된 맥락 유지 가능, 단 요약 정확도에 의존 |
| 배달 상담 적합 시나리오 | 짧은 상담(5~10턴), 빠른 해결이 목적 | 길고 복잡한 상담, 이전 문의 이력 참조가 필요할 때 |

**요약 전략이 적합한 시나리오**: 고객이 30분에 걸쳐 10턴 이상 대화하거나, 어제 문의한 건을 다시 꺼내는 경우. 슬라이딩 윈도우는 오래된 맥락을 버리므로 "저번에 말씀드린 그 건" 같은 발화에서 실패한다.

---

### "지시 대명사 해결 성공률"의 프로덕션 기준

10턴 중 7회 미만 성공이면 프로덕션에 올리면 안 된다고 생각한다.

지시 대명사를 잘못 해석하면 다른 고객 주문을 취소하거나 잘못된 정보를 안내하게 된다. 배달 상담은 금전·서비스 피해로 직결되니 10번 중 3번 틀리는 건 그냥 넘어갈 수 없다.
7회는 "이 정도면 괜찮다"가 아니라, 7회를 못 넘으면 MAX_MESSAGES 자체가 너무 작다는 신호로 읽어야 할 것 같다.

---

### 배달 상담 도메인에서 "오래된 대화"가 의미 있는 경우

1. **3시간 전 주문 건 재문의**: "아까 시킨 거 아직 안 왔어요" — 1~2시간 전 주문 orderId가 필요하다. 슬라이딩 윈도우가 짧으면 이 orderId가 소실된다.

2. **어제 환불한 건 재문의**: "어제 환불 신청했는데 아직 처리 안 됐어요" — 어제 대화가 필요하다. InMemory + 슬라이딩 윈도우로는 불가능하고 영속 저장소가 필요하다.

3. **재주문 패턴**: "저번에 시킨 거랑 똑같이 주문하고 싶어요" — 이전 세션의 메뉴 이력 참조. 고객 단위 영속 Memory가 필요한 케이스다.

---

### Memory를 세션이 아닌 고객 단위로 영속 유지할 때

**가능한 새 기능**:
- "저번에 시킨 거랑 같은 메뉴" 재주문 지원
- 단골 고객의 선호 가게 / 주소 자동 인식
- 이전 불만 기록 참조 → 보상 정책 자동 안내

**리스크**:
- 개인정보 보호법상 상담 이력 보관 기간 제한 (예: 3년)
- 고객이 "내 대화 삭제해달라"고 요청할 때 완전 삭제(Right to be forgotten) 구현 필요
- 세션 간 교차 오염: 고객 단위로 묶으면 다른 기기에서 접속한 동일 고객의 대화가 섞일 수 있다
- 저장 데이터 암호화 및 접근 제어 필요 (배달지 주소, 결제 정보 포함 가능)

---

## 자가 점검 체크리스트

- [x] 3가지 `MAX_MESSAGES` 값에 대한 정량 비교 표 작성
- [x] 실험 B에서 지시 대명사 해결 실패 예시 1개 이상 캡처 (T6 되묻기 + T10 요약 실패)
- [x] 실험 C의 "입력 토큰이 선형 증가"하는 흐름을 수치로 기록 (no-tool 턴 추이 표)
- [x] 요약 전략의 장단점 표 작성
- [x] "몇 회 미만 실패 시 프로덕션 금지" 기준 + 근거 작성
